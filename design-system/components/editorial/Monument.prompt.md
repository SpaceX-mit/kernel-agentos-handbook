Stacked issue-number corner block with the big tomato numeral — the cover's serial anchor; place bottom-right of a cover or in the colophon.

```jsx
<Monument number="360" month="APRIL" year="2026" price="¥0 · BYOK" />
```

`month`, `year`, `price` are all optional; the numeral alone works for a bare monument-hero cover.
